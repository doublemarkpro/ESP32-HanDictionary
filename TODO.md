# ESP32 汉字学习小助手：进度与周末接续清单

更新：2026-09-11，下班交接。代码基线：`ed95582`（UI4 课程表）。
仓库：<https://github.com/doublemarkpro/ESP32-HanDictionary>。
`main` 和 `codex/han-dictionary` 均已同步该代码基线；本文件是后续协作的待办入口。

**2026-09-13 真机接续**：已确认 COM7 为 Tab5 / ESP32-P4 Rev 1.3（Legacy）、16 MB Flash、
32 MB PSRAM、触摸固件版本 1 对应 ST7121（旧驱动误判为 ST7123）。首次上板修复了 SDIO 内存池分配失败，以及摄像头自动 ISP
算法非法指令导致的启动重启；ST7121 显示驱动修复后，用户确认有画面、触摸正常，
设备已配网、激活并进入待机。详见 [真机调试记录](docs/HARDWARE_BRINGUP.md)。
随后发现切页约十多秒及唤醒失败；图片缓存已将切页改善到约 2–3 秒，手动说话能回复。
`Legacy-aec-ppa` 上板后用户仍不能唤醒，也未感到进一步提速。
`Legacy-frame-diagnostics` 的全屏 PSRAM 绘制缓冲和诊断已运行，
用户确认主要切页已经很快、效果满意，但返回主页时左上角小书图标仍慢，唤醒仍未通过。
核对 ES7210 I²S TDM 时隙顺序后，确认前两轮将参考掩码改为 0+2 是错误的；
正确的 MIC1 + MIC3 参考应取时隙 0+1。`Legacy-correct-reference` 已上板，
串口已记录免按键唤醒、对话回复与查字开页链路成功。
当前已刷并校验字体、电池与天气版，应用 4,552,256 字节、槽余 690,624 字节；
返回主页合并为一次整屏提交，临时音频统计已移除。用户确认最终版切页及小书显示正常，
“你好小智”能够可靠唤醒并听到回复，这两项主要问题已通过本轮真机验收。
四个受影响 Tab5 变体均构建通过；本轮真机验收仅覆盖 Legacy 字典变体，其他三个仅验证构建。
摄像头自动图像调节暂时关闭，触摸四角、完整联网语音及 SD 内容卡仍待验收；以下交接基线保留。

> 目前是开发原型：显示、基础触摸、切页、免按键唤醒与回复已通过本轮真机验收；下列专项仍需完成。
> 下一次不要直接继续往应用里堆图片：先做真机基线和资源层改造，再扩展页面与留言板。

## 已完成

- [x] 项目 CI 收敛：日常仅 Tab5 两版、纯文档 / 截图过滤、自动取消过期运行、代码 / 资源测试及容量门槛；全板型和 S31 改为手动选择。见 [CI 说明](docs/CI.md)，云端结果以 Actions 为准。

- [x] ESP-IDF 6.0.2 环境脚本、Tab5 Legacy / P4X 两种构建配置、容量检查及独立打包脚本。
- [x] 主页 UI3：粗圆体、六入口、装饰卡片、日期时间、Wi-Fi / 电池状态图标、按住说话、返回导航。
- [x] 字体采用 Resource Han Rounded CN Heavy 子集；字体 / 笔顺许可和生成源已入库。概念图本身不是可直接使用的字体文件。
- [x] 课程表 UI4：工作日 / 周末、每天最多八节、今日高亮、本周 / 下周重复模板、明日物品翻页和临时勾选。
- [x] SD 课表校验、只读 `self.study.timetable` 查询；查询读取启动缓存，不在对话主循环中读 SD。
- [x] 字典原型：规 / 矩示例、目标字提取、SD 字条与笔顺按帧读取、MCP 查询并打开结果。
- [x] 英语音标原型：传统英式 44 音分类 / 分页、IPA 字形与例词展示、音频路径及播放接口。
- [x] 作业计时原型：语文 / 数学 / 英语互斥计时、暂停、完成、返回继续、NVS 检查点。
- [x] 闹钟原型：单个每日提醒；天气原型：显示带时间戳的 SD 缓存；网络页复用小智手机配网。
- [x] 图形包：90 项 / 252 张 PNG，约 2.91 MiB，包含多尺寸和备用素材；尚未全部接入各页面。
- [x] UI4 基线通过 78 项 Python 测试、10 项资源测试，以及真实 LVGL 桌面渲染 / 交互测试。
- [x] Legacy / P4X 均编译通过并打包；实际截图已入库，查看 [课程表](docs/ui/TIMETABLE_UI4.md) 与 [主页](docs/ui/HOME_UI3.md)。

### 不要误认为已经完成的部分

- Tab5 INA226 电池电压和充电状态已接入，COM7 实测读到 8.112 V / 约 232 mA 充电电流；电量曲线和续航仍需脱离 USB 后专项核验。
- 语义理解及语音对话仍需联网，不是完整离线小智；免按键唤醒与有声回复获用户确认，日志已记录查字开页，播放中打断/AEC、不同噪声距离及断线重连仍需专项验收。
- 正式《新华字典》第 12 版完整数据和核验页码尚未取得。示例不是该书原文，未知页码不猜测。
- 176 个教学音频目标路径已规划，但录音文件尚未提供；跟读评分未实现。
- 课表默认空白；截图使用演示数据。物品勾选不跨重启保存，周模板不处理调休；触屏编辑未实现。
- MQTT 留言板目前仅完成方案评估，尚无功能代码，也未连接 / 修改 NAS Broker。

## P0：周末先建立真机基线

- [x] 确认收到的设备确为 M5Stack Tab5，并核实 Legacy / P4X 硬件修订和串口；本次为 COM7 / Rev 1.3 / Legacy。
- [x] 按下方步骤在家中电脑准备环境、拉取代码、构建匹配版本；记录版本、完整启动日志。
- [ ] 检查无 SD 启动、横屏方向、触摸四角、六入口、返回、快速切页和按住 / 松开说话。
- [ ] 测试手机配网、断线重连、校时、音频录放、唤醒与联网查询；缺数据应提示，不编造结果。
- [ ] 用 FAT32 内容卡测试正常卡 / 缺卡 / 损坏内容，以及规 / 矩笔顺、正式课表读取；本版不支持热插拔。
- [ ] 检查课程表翻页、明日物品、跨日更新、计时掉电恢复、每日提醒实际触发。
- [ ] 在启动、进入主页、切页、语音会话、笔顺播放时记录内部 RAM / PSRAM 空闲量和最大连续块（可用 `self.study.capacity`）。
- [ ] 核验电池接口、充电行为、休眠和续航；关机闹钟 / RTC 唤醒不能视为已支持。

完成标准：留下可复现的测试记录和问题清单；编译通过不替代任何硬件项目的勾选。

## P0：统一资源层与字体接入（下一项开发主线）

### 当前容量基线

| 项目 | 实测值 |
| --- | ---: |
| P4X 应用 | 4,618,240 字节 |
| P4X 5 MiB 应用槽余量 | 624,640 字节 = 610 KiB |
| 保留 512 KiB 后的新增预算 | 98 KiB |
| Legacy 应用 / 余量 | 4,594,448 / 648,432 字节 |
| 内置资源区容量 / 已用 | 6,160,384 / 2,985,897 字节 |
| 内置资源区余量 | 3,174,487 字节，约 3.03 MiB |

现有资源包中 `font_noto_sans_common_30_4.bin` 为 2,609,092 字节，`srmodels.bin` 为 291,042 字节。
这套常用正文中文字库已经存在，但产品页面仍直接引用独立子集，尚未接入留言正文；必须检查字形覆盖，不能承诺全汉字。
分区标记虽为 SPIFFS，实际是小智自定义索引资源包，不是可直接复制文件的文件系统。

| 存储层 | 计划职责 |
| --- | --- |
| 应用固件 | 页面 / 业务代码、必要操作图标、故障提示小字库、资源缺失回退 |
| 内置资源区 | 无卡也要漂亮的核心页面插画、常用天气图标、粗圆体子集、常用正文中文 |
| SD | 大型主题 / 字库、字典 / 笔顺 / 教学音频、留言历史与附件 |
| PSRAM | 有上限的当前页面解码缓存、字形缓存、消息缓冲，不作持久存储 |
| NAS | 留言原始记录、附件与投递 / 已读状态，用于补发和恢复 |

- [ ] 建立统一资源 ID / 索引和加载接口，支持内置资源区、SD、必要内置回退；不让页面直接依赖存储路径。
- [ ] 扩展产品资源打包，复用 `Assets::GetAssetData`；只打包实际使用的尺寸，不把完整 2.91 MiB 多尺寸素材库全部塞入资源区。
- [ ] 迁移主页 / 课程表中的合适图片及较大字体，保留选定粗圆体的视觉效果、4 bpp 字体质量和无卡启动能力。
- [ ] 验证二进制字体转换与加载格式匹配、生命周期正确；接入常用正文中文及缺字回退，解决远程留言任意文本可能出现方框的问题。
- [ ] 工作线程读卡 / 加载、进入页面预备、离开页面释放；设置解码 / 字形缓存上限，避免抢占语音任务。
- [ ] 加入应用与资源版本匹配、完整性校验、缺文件 / 坏包回退；资源区只有一份，需处理断电和映射指针失效，不得运行中随意覆盖。
- [ ] 防止普通小智资源更新覆盖产品资源；第一阶段采用统一打包 / 受控更新，不擅自改分区或删除语音 / OTA。
- [ ] 两种板型重新编译、对比 map / `.bin`，模拟器截图回归，再测真机切页与语音同时运行。

规划目标（不是已实现结果）：净回收约 450–600 KiB，应用空闲达到至少 1 MiB；后续页面与文字留言板先按合计
400 KiB 新增应用预算管理；首阶段新增核心图形资源控制在 1 MiB 内，始终保留至少 512 KiB 应用安全余量。
若实测不达标，重新评估字体 / 组件 / 存储分工，不降低门槛掩盖问题。详见 [体积说明](docs/FIRMWARE_SIZE.md)。

## P1：NAS MQTT 文字留言最小闭环

已知：家长已在 NAS 部署 Mosquitto；Broker 版本、网络入口、证书和账号尚未核实。
先按文字留言规划，语音附件放后续，不替换小智对话使用的连接与配置。

- [ ] 联调前确认 Broker 版本、家中 / 外网连通方式、TLS 或 VPN、设备独立账号及主题 ACL；凭据本地配置，不写代码、不进 Git。
- [ ] 定义消息 ID、发送者、正文长度上限、时间 / 有效期、重复消息处理及协议版本。
- [ ] 独立 MQTT 客户端 / 命名空间、限长队列、退避重连、超时；补齐现有封装未暴露的必要证书 / 会话配置，不修改 vendor 生成目录。
- [ ] NAS 保存留言与状态，设备上线补收；区分 MQTT 送达、设备持久保存、孩子点了已读，不能将其混为一个回执。
- [ ] SD 保存有条数 / 总容量限制的历史，消息 ID 去重；测试无 SD、满卡、坏卡、断电，不假报“已保存”。NVS 只有 16 KiB，不拿它存整套历史。
- [ ] UI：顶部信封与未读角标、留言列表、详情、“我知道啦”回执；保留现有六张首页大卡片（布局仍待实际设计验收）。
- [ ] 默认轻提示，不强切计时 / 学习 / 对话页面；加入静音 / 勿扰规则。
- [ ] 测试设备离线多条留言、NAS / Broker 重启、重复补发、过期留言、异常 / 超长内容、已读状态恢复。
- [ ] 首先用 MQTT 测试客户端跑通闭环，再补家长手机操作页面与 NAS 留言服务部署说明。
- [ ] 后续可选语音留言：MQTT 传附件元数据，HTTPS 下载到 SD 后播放；不将大段音频放入 MQTT 正文。

Mosquitto 持久化、主题权限和 TLS 的配置需按实际版本核对；QoS / retained 不等于完整留言历史和孩子已读。
参考：[Mosquitto 官方配置说明](https://mosquitto.org/man/mosquitto-conf-5.html)。

## P1 / P2：其余页面与内容

- [ ] 全屏翻页时钟：主页点击顶部时间进入，显示时分秒、公历年月日、星期与农历信息；实现平滑进位翻页且保持静音。已确认的设计稿、动效要求和验收标准见 [翻页时钟待办](docs/ui/FLIP_CLOCK_TODO.md)。
- [ ] 字典页：按设计细化查询结果、田字格、笔顺控制、释义和页码状态；扩充合法字条 / 笔顺与字体覆盖。
- [ ] 新华字典：固定商务印书馆第 12 版单色本，ISBN **978-7-100-16807-6**；取得可用内容并逐项核验纸书页码，不以其他版本代替。
- [ ] 英语音标页：统一分类 / 卡片 / 播放状态与背景，补齐并核对教学录音；跟读录音 / 评分另行评估。
- [ ] 课程表：按真实课程填写 SD；后续增加触屏编辑、物品勾选持久化、日期例外 / 调休规则。
- [ ] 作业计时：细化科目选择、进行中 / 暂停 / 完成页，增加每日 / 每周历史及统计展示。
- [ ] 闹钟：细化时间设置 / 响铃 UI、多闹钟 / 重复日期 / 稍后提醒；RTC 和关机唤醒单独验证。
- [ ] 天气：和风天气实时天气 v1、城市配置、更新时间和离线缓存已接入；待用真实 API KEY 联调，并补不同天气图标和预报卡片。
- [x] 内容卡传输：安全的 USB-C MSC 读卡器模式已实现并在 COM7 真机枚举 128 GB 卡；切换前停止内容读取并卸载应用侧文件系统，电脑安全弹出后重启恢复。
- [ ] 内容卡准备：当前 128 GB 卡确认是 exFAT；先备份，再格式化为 FAT32 并复制 `handict`，完成字条、笔顺、课表和天气配置真机读取。
- [ ] 联网 / 设置：细化网络状态、重连 / 错误提示；按需要补触屏选网键盘、音量 / 亮度 / 勿扰。
- [ ] 全部页面统一返回、状态栏、字体、触摸区域、按压反馈和加载 / 缺资源状态；每完成一页复查容量与真机性能。

## 回家电脑怎么接着做

只看进度 / 效果图时，直接在 GitHub 打开本文件和下方文档即可，不需要安装 SDK。
要写代码和构建时：安装 Git、ESP-IDF **6.0.2** 及 ESP32-P4 工具链；首次拉取依赖需要网络。
家中仓库路径可以不同，不需要复制公司的 `build/`、`managed_components/`、Python 环境或 SDK 目录。

首次克隆（在选好的父目录执行）：

```powershell
git clone https://github.com/doublemarkpro/ESP32-HanDictionary.git
cd ESP32-HanDictionary
git switch codex/han-dictionary
```

已有仓库：先 `git status`，确认并保留本地改动后，在同一分支执行 `git pull --ff-only`。
若提示分叉或存在冲突，停下来一起处理，不用 reset / 强推覆盖另一台电脑的工作。

在仓库根目录：

```powershell
. .\tools\setup.ps1
# 找不到 SDK 时，使用 setup.ps1 的 -IdfPath 参数指向家中实际安装目录。
# 核实硬件后仅选择匹配版本；下面 P4X 是示例，Legacy 设备将参数改为 Legacy。
.\tools\build.ps1 -HardwareRevision P4X
python tools/capacity_report.py
python -m unittest discover -s scripts/tests -v
```

若使用非默认 SDK 路径，`build.ps1` 也支持 `-IdfPath`。烧录前再次确认当前构建板型与串口；本清单不自动发起刷机。
公司的最终烧录包在 `dist/firmware/ui4-final-Legacy` / `ui4-final-P4X`，**这些二进制没有上传 GitHub**；家中重新构建。
生成后的字体 / 图标 C 源和所需原始素材已入库，普通固件构建无需 Node.js。

准备 SD 内容包（输出目录须不存在）：

```powershell
python tools/content_pack.py prepare --output dist/weekend-card
python tools/content_pack.py validate dist/weekend-card/handict
```

把 `handict` 目录放到卡根目录；正式课表由家长填写，不直接采用截图中的演示课表。

需要重新生成图形或运行桌面预览时，另安装 Node.js；预览还需 Visual Studio C++ 桌面构建工具和 Windows SDK。
先完成一次固件依赖拉取，再执行：

```powershell
npm ci --prefix tools/ui-assets
node --test tools/ui-assets/*.test.cjs
.\tools\preview-ui.ps1 -OutputPath dist/weekend-preview
```

### 周末开始时可以直接说

> 请先读 TODO.md 和 docs/DEVELOPMENT_STATUS.md，检查当前分支 / 工作区。先帮我确认真机修订和构建环境，
> 完成基础验收，再从 P0 资源层改造开始；保留粗圆体效果，不擅自改分区或删除语音 / OTA 功能。

更多资料：[主页 UI3](docs/ui/HOME_UI3.md) · [课程表 UI4](docs/ui/TIMETABLE_UI4.md) ·
[最新状态与历史](docs/DEVELOPMENT_STATUS.md) · [SD 内容说明](docs/SD_CARD.md) · [图形素材](docs/ui/GRAPHICS.md)。
