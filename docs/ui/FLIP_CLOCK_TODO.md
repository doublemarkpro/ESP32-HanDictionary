# 全屏翻页时钟：设计与待办

状态：**固件实现完成，待 Tab5 真机验收**。

## 已确认设计

![全屏翻页时钟设计稿](flip-clock-preview/flip-clock-concept-v2.png)

- 从主页点击顶部时间进入，作为独立的全屏时间展示页面。
- 页面主体只显示时、分、秒；左上角保留统一返回按钮。
- 时钟下方显示公历年月日、星期和农历信息。设计稿中的日期仅为排版示例，实际显示必须读取设备当前日期。
- 延续天气、字典和设置页面的柔和背景、深蓝主色及大字号风格。
- 不显示标题、状态栏、天气、语音入口或其他操作按钮，避免干扰时间阅读。

## 动效与交互待办

- [x] 在主页顶栏时间区域增加点击入口，并保持原有时间刷新与其他顶栏交互正常。
- [x] 新增全屏翻页时钟页面及统一返回路径。
- [x] 实时显示 `HH:MM:SS`，补零并使用 24 小时制。
- [x] 秒钟每秒执行一次翻页；分钟和小时只在进位时翻页。
- [x] 翻页总时长 300 ms：旧数字上半页向内翻下，再展开新数字下半页。
- [x] 使用轻微明暗和缩放表达翻页，不绘制突兀的黑色中心横线。
- [x] 显示公历年月日、星期及农历年月日；日期跨日时自动刷新。
- [x] 页面进入、退出及动画过程中不阻塞 LVGL、主事件循环或音频任务。
- [x] 禁止声音反馈；时钟页面及调试过程保持静音。
- [ ] 在桌面模拟器检查布局和动效，再在 Tab5 真机验证帧率、触摸返回、跨分钟及跨日更新。

## 实现说明

- 时钟直接读取系统墙上时间，每 100 ms 检查一次秒值，不用定时器累加秒数，避免长期累计漂移。
- 六张数字卡片分别维护状态，只对变化的位启动 LVGL 动画；进入页面时直接呈现当前时间。
- 农历转换内置 1900—2100 年数据，超出支持范围时显示明确的不可用提示。
- 专用字体可用 `node tools/ui-assets/flip-clock-assets.cjs` 重新生成。

## 验收标准

- 六位数字在 1280×720 横屏中保持水平居中、间距一致，没有裁切或跳位。
- 静止状态数字清晰；翻页时只改变需要进位的数字卡片。
- 日期信息位于时钟正下方，层级弱于时间但在正常观看距离下可读。
- 从主页点击时间能够稳定进入，点击返回按钮能够立即回到主页。
- 连续运行不会出现累计计时漂移、动画重叠或明显内存增长。
