# 第二轮：资源、容量与逻辑状态

历史开发基线：2026-09-11；真机接续：2026-09-13。这里记录实现状态，不以概念图或编译通过代替真机验收。

**2026-09-13 真机更新**：已在 COM7 的 Tab5 / ESP32-P4 Rev 1.3（Legacy）完成首次上板，
识别 ST7121 并修复启动、显示及刷新问题。当前 USB-C MSC 版应用 4,608,096 字节，
5 MiB 应用槽余 634,784 字节；用户确认显示、基础触摸、切页、小书同步显示、免按键唤醒及有声回复正常，
串口记录了字典查询开页链路。84 项 Python 主机测试和四个 Tab5 变体构建均通过，
本轮真机验收仅覆盖 Legacy 字典变体，其他三个仅验证构建。
动态回复已切换到通用 Noto 字体，INA226 实测 8.141 V / +305 mA，和风天气 v1 已接入。
128 GB 卡已通过 USB-C MSC 枚举为 119.25 GB 可移动磁盘，并确认原格式为 exFAT；备份后改为 FAT32、
真实天气凭据、长时间稳定性和播放中打断/AEC 仍待测试；
详细固件、日志和验收边界见 [真机调试记录](HARDWARE_BRINGUP.md)。
以下 UI2/UI3/UI4 的旧体积、包名和测试数量保留为 2026-09-11 历史基线，不代表当前固件结果。

后续开发优先级、资源迁移 / MQTT 留言板规划及回家接续步骤统一见 [项目 TODO](../TODO.md)。

**UI4 更新（2026-09-11）**：课程表已换为实际 LVGL 表格，支持工作日 / 周末、八节课翻页、临时物品勾选，
并加入只读课程查询 MCP。SD 模板仍为空，不把演示课程当成正式内容。
P4X 应用 4,618,240 字节，比 UI3 增加 51,056 字节，5 MiB 应用槽余 624,640 字节。
Legacy 应用 4,594,448 字节，余 648,432 字节。两版均已编译、通过容量检查，最终烧录包位于
`dist/firmware/ui4-final-P4X` 和 `dist/firmware/ui4-final-Legacy`；旧包原样保留。
78 项 Python 测试、10 项资源测试及原生 LVGL 交互 / 截图测试通过。
该阶段语音端到端、跨日、SD 真机与性能尚待验收；后续真机结果见上方更新。
[实际界面和格式](ui/TIMETABLE_UI4.md)、
[固件体积说明](FIRMWARE_SIZE.md)。

**UI3 更新（2026-09-11）**：真实 LVGL 主页已接入资源圆体 Heavy、吉祥物与六张装饰卡片、日期/时间、
Wi-Fi/电池状态和按住说话。Legacy 应用 4,543,504 字节，剩 699,376 字节。
P4X 应用 4,567,184 字节，剩 675,696 字节；两个版本均已编译和打包。
Tab5 电量接口尚未实现，因此显示未知图标；录音和性能在该阶段尚待真机测试。
详见 [UI3 实现、截图与验证](ui/HOME_UI3.md)。下面 UI2 数据保留为历史基线。

UI3 之前的图形包阶段：按概念图完成 90 项/252 张 PNG 素材，含六入口插画、22 种天气、
50 个小图标、背景与学习装饰；全部 PNG 约 2.91 MiB。另提供约 90 KiB PNG 载荷的
可选 LVGL C 小图标包，并经过实际 LVGL 解码/渲染验证。该准备阶段尚未替换固件页面皮肤，
后续 UI3 已接入主页素材。清单、图集、内存预算和调用方式见 [图形素材说明](ui/GRAPHICS.md)。

## 容量结论

现阶段可以继续推进学习工具，但必须保持“代码和小型 UI 资源在 Flash，大型字库/音频/笔顺在 SD”的分工。Tab5 官方配置为 [16 MB Flash、32 MB PSRAM](https://docs.m5stack.com/en/core/Tab5)。PSRAM 是运行内存，不是可断电保存的容量。

UI2 历史编译结果（2026-09-11）：P4X 应用 4,370,752 字节，5 MiB 分区剩 872,128 字节（16.6%，约 852 KiB）；相比上一轮应用减少 292,864 字节。保留 4 bpp 的字体无损压缩，同时增加了 44 音卡片、矩字、SD 笔顺和诊断工具。压缩效果经过实际 LVGL 栅格解码及截图检查。

Legacy 版本也已编译通过：应用 4,347,072 字节，剩 895,808 字节（17.1%，约 875 KiB）。独立烧录包位于本机 `dist/firmware/ui2-P4X` 与 `dist/firmware/ui2-Legacy`，未改变上一轮分区布局。

资源分区容量 6,160,384 字节，现有小智资源占 2,985,897 字节，剩 3,174,487 字节（约 3.03 MiB）。这部分不能直接当作应用增长空间；要改变分工需重做分区布局并完整刷机。另一个 5 MiB OTA 槽是升级保留空间，不能作为随意存放字库的空闲空间。

2026-09-13 已取得启动、切页及语音阶段的 RAM 空闲量采样，见真机记录。
不同内容卡、笔顺和点读负载下的最大连续块与长期碎片情况仍需测试。
只读 MCP `self.study.capacity` 返回内部 RAM / PSRAM 的空闲总量、最大连续块、当前应用分区大小和 SD 内容状态，不能用“32 MB 总量”替代这些测量。

```powershell
python tools/capacity_report.py
```

容量脚本已接入 `tools/build.ps1`，读取当前构建选中的实际 CSV、烧录参数及二进制文件，默认应用至少保留 512 KiB，分区溢出直接失败。这是工程安全下限，不代表未来任意功能都能放下。完整离线语音大模型不是当前方案。

## 代码逻辑完成度

| 模块 | 当前真正实现 | 尚未完成 / 待测 |
| --- | --- | --- |
| 主页/导航 | 六入口、返回、状态栏、点击语音；横屏、基础触摸与切页已真机确认 | 触摸四角精度、完整交互与长期使用、动效及插画细化 |
| 查字 | 目标单字提取、SD 字条、ISBN/核验页码规则、MCP 打开结果 | 完整新华字典数据、页码、完整任意中文字体；通用语义仍联网 |
| 笔顺 | 内置规字；SD 按帧加载其他字；暂停/前后步；过期结果丢弃 | 大规模合法笔顺来源与真机 I/O 性能；当前示例为规、矩 |
| 音标 | 传统英式 44 音卡、12/8/24 分类、每页六卡、132 个例词位置、点读路径 | 176 个录音文件待提供/核对；录音回放和评分尚未接入 |
| 课程表（UI4） | SD 周课表、今日高亮、周末 / 八节翻页、明日物品临时勾选、缓存 MCP 查询 | 屏上编辑、勾选持久化、调休规则、真机验收 |
| 作业计时 | 语文/数学/英语互斥计时、暂停、完成、返回继续、NVS 检查点 | 每日/每周历史，NVS 掉电恢复真机验收 |
| 闹钟 | 单个每日提醒、保存/关闭、开机且时间已同步时触发 | RTC 时间/关机唤醒、多闹钟、稍后提醒、实际铃声体验 |
| 天气 | 和风实时天气 v1、私有 API 配置、手动刷新、更新时间和离线缓存 | 真实凭据联调、预报卡片、定位/家长城市配置 |
| 网络 | 复用小智手机配网，已完成联网及语音对话基础验证 | 屏上 Wi-Fi 列表/键盘；专门的断线重连、播放中打断和长期稳定性 |
| USB 内容卡 | Legacy 字典变体可将 microSD 安全切换到 USB-C MSC；128 GB 卡已完成 Windows 枚举 | 安全弹出后重启、FAT32 格式化与内容读回 |

## 已准备的资源

- 压缩 UI 字体 28/40 px、完整本内容包的 IPA 大字；已覆盖 `θ`、`ð`、`ŋ`、`ɡ`、长度符号等。不是全汉字字体。
- 六个原创矢量图标及 PNG；沿用现有素材系统，不把概念图当作实际控件。
- 可生成 44 张 SVG/PNG 音标学习卡，文字转为 DejaVu 字体轮廓，避免其他电脑缺字体。这些是预览/后续素材，固件当前用 LVGL 文本直接绘制，不加载整张卡片图片。
- 规、矩共 17 张 300×300 笔顺帧；矩字通过 SD 演示，释义为项目编写示例，不是新华字典原文。
- 字体/笔顺许可证、原始路径数据和可复现生成脚本。音频目录清单有 176 个目标路径，但不包含教学录音。

基础 SD 资源已经随仓库提交，普通准备只需 `python tools/content_pack.py prepare --output dist/my-card`，无需安装 Node.js。下方命令用于重新生成/扩展素材：

```powershell
npm ci --prefix tools/ui-assets
node tools/ui-assets/generate.cjs
node tools/ui-assets/prepare-resources.cjs --output dist/sdcard-ui2
.\tools\preview-ui.ps1 -ContentPath D:\ESP32Projects\ESP32-HanDictionary\dist\sdcard-ui2\handict
```

输出目录须不存在；2026-09-11 的 UI2 内容包为 `dist/sdcard-ui2-final/handict`，约 1 MB。
将整个 handict 目录复制到卡根目录。家中路径不同就调整示例 `ContentPath`，脚本自身不绑定公司路径。

自有结构化条目可通过 `--entries local-dictionary.json` 导入，已有使用权的 Hanzi Writer 格式路径可通过 `--strokes-dir local-strokes` 导入。格式校验不代表对内容质量、笔顺标准或版权进行了自动认证；自定义来源需补齐自己的授权与署名。任意新增中文文本仍需重新生成字体或等待后续 SD 字体扩展。

## 验证与下一步

2026-09-11 的 UI2 增加容量溢出/安全余量/路径检查、PNG 尺寸/截断/CRC/帧数检查；LVGL 模拟器检查全部音标字形、实际字体解码、标题像素、分页循环、SD 笔顺显示，以及快速换帧/离开页面时丢弃旧结果。
2026-09-13 已完成上述基础显示、切页及语音真机验收；INA226 已读到有效电压/电流，
动态文本字体和待机残字已修复，USB-C 读卡器已完成真机枚举。电池图标人工确认、
FAT32 内容卡、长期稳定性及音频边界场景仍未完成。

UI2 历史基线为 75 项 Python 测试通过；本轮为 84 项，四个 Tab5 变体构建均通过，范围以上方真机更新为准。
原版小智 MCP 分发仍同步调用字条查询（最大 16 KiB），慢卡下的延迟需要测量并进一步异步化；笔顺图片和音频读取已经在独立工作线程。当前没有修改共用 MCP/网络协议。

下一优先级：备份并将 128 GB 卡格式化为 FAT32、导入内容包、真实和风天气联调、长时间运行与音频边界场景验收 → 字典全量资源与外置字体方案 → 教学录音与跟读 → 课程编辑/计时历史/RTC 提醒。没有取得资料前，不生成虚构的新华字典页码或把普通 TTS 输出当作标准音标录音。
